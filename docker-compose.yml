version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - templates:/app/templates
      - generated_docs:/app/generated_docs
      - ./templates.db:/app/templates.db

volumes:
  templates:
  generated_docs: 